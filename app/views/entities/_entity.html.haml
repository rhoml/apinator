%tr
  %td= entity.name
  %td
    = link_to 'edit', edit_project_entity_path(@project, entity)
    |
    = link_to 'destroy', project_entity_path(@project, entity), method: :delete, data: {confirm: 'Are you sure?'}
